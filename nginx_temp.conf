events {
    worker_connections 1024;
}

http {
    # ТОЛЬКО HTTP сервер для ACME challenges
    server {
        listen 80;
        server_name 72-56-72-131.nip.io;

        # Обязательно для certbot
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
            try_files $uri =404;
        }

        location / {
            return 404;
        }
    }
}